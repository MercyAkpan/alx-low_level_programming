#include <stdio.h>
/**
  * main -ENTRY POINT
  * Return: 0 on SUCCESS
  */
int main(void)
{
	int a, b;

	for (a = 48; a <= 57; a++)
	{
	putchar(a);
	}
	for (b = 97; a <= 102; a++)
	{
	putchar(b);
	}
	putchar('\n');
	return (0);
}
