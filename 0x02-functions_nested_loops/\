#include "main.h"
/**
  * _islower -This checks if a chacter is lowercase or not
  * Return: 1 if charcter is lowercase otherwise return 0
  */
int _islower(int c)
{

if (c >= 97 && c <= 122)
{
	return (1);
}
else
{
	return (0);
}
}
